-- This scene is only used as an overlay to prevent the user from being able to interact with the piano keys 
-- used during the demo and at other points of the game
local composer = require( "composer" )
local scene = composer.newScene()
 
return scene